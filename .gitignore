# Dependencies
node_modules/

# Secrets — API keys live here, never commit
.env
.env.*
!.env.example

# Persistence (SQLite) — generated at runtime, never commit
data/

# Auto-generated caches
resort-cache.json

# OS / editor noise
.DS_Store
Thumbs.db
